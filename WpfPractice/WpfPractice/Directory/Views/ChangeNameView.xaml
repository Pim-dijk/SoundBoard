<Window x:Class="WpfPractice.Views.ChangeNameView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfPractice.Views"
        mc:Ignorable="d"
        Title="ChangeNameView" Height="150" Width="300" ShowInTaskbar="False" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" FontFamily="Tahoma" FontSize="11">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="4" Text="Change the name to:" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <TextBox x:Name="textField" Grid.Row="1" Grid.ColumnSpan="4" Text="{Binding Path=NameToChange, Mode=TwoWay}" Margin="15, 0, 15, 0" Height="30" FontSize="17" />

        <Button Grid.Row="2" Grid.Column="0" Content="Ok" Command="{Binding ChangeSoundNameSaved}" Margin="9, 5, 0, 5" Width="45"/>
    </Grid>
</Window>
